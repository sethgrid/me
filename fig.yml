web:
  build: .
  command: hugo server --theme=nofancy --buildDrafts --baseUrl=http://104.131.9.167:1313
  ports:
   - "1313:1313"
  # volumes:
  #  - .:/main
  links:
   - redis
redis:
  image: redis
  ports:
   - "6379"
