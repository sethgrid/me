web:
  build: .
  command: hugo server --theme=nofancy --buildDrafts --baseUrl=http://sethammons.com
  ports:
   - "1313:1313"
  # volumes:
  #  - .:/main
  links:
   - redis
redis:
  image: redis
  ports:
   - "6379"
